# Invigilant

The portable oversight core: one Rust implementation of the deterministic danger gate, the heartbeat/light state machine and the reviewer bus, replacing the divergent bash and PowerShell ports. Cross-platform (macOS and Windows first), installable, agent-agnostic. Apache-2.0, public from commit 1.

## Quick start

```bash
# TODO: install / run / test commands (filled by /project)
```

---
_Project created 2026-08-01 via the Mac Management project-system._
